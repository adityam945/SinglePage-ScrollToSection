import React from "react";

export default function Section() {
  return (
    <div>
      <p>
        Hello,This defines a single page scroll and nav bar with links(or
        buttons)
      </p>
      <p>Uses Material-ui library</p>
    </div>
  );
}
